# Prometheus configuration for Roil
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'roil-finance-backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['rebalancer-backend:3001']
        labels:
          service: 'rebalancer-backend'
          network: 'canton'

  - job_name: 'roil-finance-nginx'
    metrics_path: '/stub_status'
    static_configs:
      - targets: ['rebalancer-frontend:8080']
        labels:
          service: 'rebalancer-frontend'
